import musicMappings from "./musicMap.json" with { type: "json" };
import emblemMappings from "./emblemMap.json" with { type: "json" };
import albumCoverMappings from "./albumCoverMap.json" with { type: "json" };
import artistMappings from "./artistMap.json" with { type: "json" };
import musicNameMappings from "./musicNameMap.json" with { type: "json" };
import bonusMap from "./bonusMap.json" with { type: "json" };

export { musicMappings, emblemMappings, albumCoverMappings, artistMappings,  musicNameMappings, bonusMap };
